{
  "name": "@bcdapps/icons",
  "version": "1.0.0",
  "sideEffects": false,
  "private": true,
  "main": "dist/index.js",
  "module": "dist/icons.esm.js",
  "types": "dist/index.d.ts",
  "typings": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT",
  "scripts": {
    "prebuild": "yarn typecheck",
    "build": "concurrently yarn:build:*",
    "build:cjs": "cross-env BABEL_ENV=cjs babel src --root-mode upward --extensions .ts,.tsx -d dist/cjs --plugins=inline-react-svg  --source-maps --copy-files",
    "build:esm": "cross-env BABEL_ENV=esm babel src --root-mode upward --extensions .ts,.tsx -d dist/esm --plugins=inline-react-svg  --source-maps --copy-files",
    "build:types": "tsc --emitDeclarationOnly --declaration --declarationDir dist/types",
    "typecheck": "tsc -p tsconfig.json"

  },
  "devDependencies": {
    "babel-plugin-inline-react-svg": "^1.1.1"
  }
}
